import styled from "styled-components";

export const ContainerStyle = styled.main`
    max-width: 80em;
    width: 80%;
    margin: auto;
    display: flex;
    flex-direction: column;
    gap: 1em;
`